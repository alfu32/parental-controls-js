#!/bin/bash

CDIR=´pwd´
eval "export $(egrep -z DBUS_SESSION_BUS_ADDRESS /proc/$(pgrep -u $LOGNAME gnome-session)/environ)"
cd $SUPER_HOME # /home/alfu64/parental-controls/

DATE=`date +'%Y/%m/%d'`
LOGS="logs/$DATE"

./parental-controls-x86_64-unknown-linux-gnu.bin

cd $CDIR
